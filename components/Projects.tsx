import React from "react";
import SectionTitle from "./SectionTitle";

const Projects = () => {
  return (
    <section>
      <SectionTitle title="Projects"></SectionTitle>
      <h1 className="font-bold text-2xl">NewsPaper Notes</h1>
    </section>
  );
};

export default Projects;
